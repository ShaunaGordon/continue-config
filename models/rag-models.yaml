%YAML 1.1
---
# Models specifically for RAG, summarization, and question answering

name: RAG Models
version: 1.0.0
schema: v1

model_defaults: &model_defaults
  provider: openai
  apiKey: EMPTY
  apiBase: http://localhost:1337/v1

models:
  - name: Command-R
    <<: *model_defaults
    model: command-r:32b
