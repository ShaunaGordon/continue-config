%YAML 1.1
---
name: O3DE Assistant
version: 1.0.0
schema: v1

models:
  - uses: ~/.continue/localHub/models/autocomplete.yaml
  - uses: ~/.continue/localHub/models/embedding.yaml
  - uses: ~/.continue/localHub/models/autodetect.yaml

context:
  - provider: code
  - provider: codebase
  - provider: currentFile
  - provider: diff
  - provider: docs
  - provider: folder
  - provider: open
  - provider: problems
  - provider: search
  - provider: terminal

# mcpServers:
#   - uses: ~/.continue/pandoc.yaml
